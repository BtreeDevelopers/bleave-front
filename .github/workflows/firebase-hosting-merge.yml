# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
env:
  VUE_APP_API_URL: ${{ vars.DEV_VUE_APP_API_URL }}
  VUE_APP_API_URL_BAUTH: ${{ vars.DEV_VUE_APP_API_URL_BAUTH }}
  VUE_APP_APPLICATION: ${{ vars.DEV_VUE_APP_APPLICATION }}
  VUE_APP_BAUTH: ${{ vars.DEV_VUE_APP_BAUTH }}
  VUE_APP_IV_CRYPTO: ${{ vars.DEV_VUE_APP_IV_CRYPTO }}
  VUE_APP_SECRET_CRYPTO: ${{ vars.DEV_VUE_APP_SECRET_CRYPTO }}
"on":
  push:
    branches:
      - dev
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_BLEAVEDEV }}"
          channelId: live
          projectId: bleavedev
